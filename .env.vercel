# =====================================================
# VERCEL ENVIRONMENT VARIABLES
# =====================================================
# Instructions:
# 1. Fill in your actual values below
# 2. In Vercel dashboard, go to: Settings → Environment Variables
# 3. Click "Import .env File" and upload this file
# 4. Select all three environments: Production, Preview, Development
# =====================================================

# REQUIRED - Supabase Configuration
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# REQUIRED - JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-minimum-32-characters
JWT_REFRESH_SECRET=your-refresh-token-secret-also-minimum-32-characters

# REQUIRED - CORS (Update after first deployment with your actual Vercel URL)
CORS_ORIGIN=https://uqudo-admin-portal.vercel.app

# Environment
NODE_ENV=production

# OPTIONAL - JWT Token Lifetimes (defaults shown)
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# OPTIONAL - Rate Limiting (defaults shown)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# OPTIONAL - File Upload Limits
MAX_FILE_SIZE_MB=100
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png,xls,xlsx,csv

# OPTIONAL - Logging
LOG_LEVEL=info

# =====================================================
# UQUDO SDK INTEGRATION (for QR-based verification)
# =====================================================

# Uqudo API Credentials (REQUIRED for production SDK flows)
# Get these from Uqudo Dashboard: https://dashboard.uqudo.com
UQUDO_CLIENT_ID=your-uqudo-client-id
UQUDO_CLIENT_SECRET=your-uqudo-client-secret
UQUDO_AUTH_URL=https://auth.uqudo.com/oauth2/token

# QR Verification Configuration
QR_TOKEN_EXPIRY_MINUTES=5
DEFAULT_JOURNEY_ID=your-default-journey-id

# Deep Link Configuration
DEEP_LINK_SCHEME=uqudo
UNIVERSAL_LINK_BASE=https://uqudo.app/verify
